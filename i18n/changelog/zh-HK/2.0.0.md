---
version: 2.0.0
date: 2024-05-29
title: "若 `CPU` 不受支援"
source: de2d59c8648e
---

## New

- 在發行版本中加入 `Nvidia`
- 為較舊的 CPU 新增發行版本

## Improved

- 在 `macOS` 上為 `GPU` 啟用 `metal` 框架,速度提升約 `~40%`
- 更新至 `whisper.cpp` 版本 `1.6.2`

## Fixed

- 若 `CPU` 不受支援——顯示錯誤訊息,並在 `Windows` 上開啟修正用的 `URL`
