---
version: 1.0.7
date: 2024-05-20
title: "修正即時預覽中的 not a number JS 錯誤"
source: 0d83de7467ef
---

## New

- 改善設計,滑鼠移到音訊播放器上時檔名會變成連結
- 在 `Windows` 上執行以下指令即可在 `cmd.exe` 中顯示日誌:
- 新增日誌,檢查 CPU 是否支援 `f16c` 指令
- 使用 `showfile` crate,在檔案總管中以選取狀態開啟檔案
- 新增 `Nvidia` 版本(1 小時的內容不到 5 分鐘就能轉錄完成)

## Fixed

- 修正即時預覽中的 not a number JS 錯誤(感謝 @Ifan24 回報)
- 修正開啟設定時預設轉錄語言被更改的問題,現在會保持不變
- 新增不含 `avx` 的版本,避免舊款 CPU 當機
