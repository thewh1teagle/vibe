---
version: 1.0.7
date: 2024-05-20
title: "Se corrigió el error de JS 'not a number' en la vista previa en tiempo real"
source: 0d83de7467ef
---

## New

- Se mejoró el diseño convirtiendo el nombre del archivo en el reproductor de audio en un enlace al pasar el cursor
- Muestra los registros en `Windows` ejecutando lo siguiente en `cmd.exe`:
- Se agregaron registros para verificar si la CPU soporta la instrucción `f16c`
- Se usa el crate `showfile` para abrir los archivos en estado seleccionado en el explorador de archivos
- Se agregó el binario de `Nvidia` (transcribe 1 hora en menos de 5 minutos)

## Fixed

- Se corrigió el error de JS 'not a number' en la vista previa en tiempo real (gracias a @Ifan24 por reportarlo)
- Se corrigió que al abrir configuración se mantiene el idioma de transcripción predeterminado en lugar de cambiarlo
- Se agregó un binario sin `avx` en caso de fallo en CPUs antiguas
