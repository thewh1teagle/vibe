---
version: 1.0.7
date: 2024-05-20
title: "修复了实时预览中的 not a number JS 错误"
source: 0d83de7467ef
---

## New

- 改进了设计，鼠标悬停时音频播放器中的文件名会显示为链接
- 在 `Windows` 上通过在 `cmd.exe` 中执行以下命令查看日志：
- 新增日志，用于检测 cpu 是否支持 `f16c` 指令
- 使用 `showfile` crate，在文件管理器中以选中状态打开文件
- 新增 `Nvidia` 版本（转录 1 小时音频耗时不到 5 分钟）

## Fixed

- 修复了实时预览中的 not a number JS 错误（感谢 @Ifan24 的反馈）
- 修复了打开设置时会改变默认转录语言的问题，现在会保持不变
- 新增非 `avx` 版本，以应对旧款 CPU 崩溃的情况
