---
version: 2.0.0
date: 2024-05-29
title: "当 `CPU` 不受支持时"
source: de2d59c8648e
---

## New

- 在发布版本中新增 `Nvidia`
- 新增面向旧款 CPU 的发布版本

## Improved

- 在 `macOS` 上为 `GPU` 启用 `metal` 框架，速度提升约 `~40%`
- 更新到 `whisper.cpp` 版本 `1.6.2`

## Fixed

- 当 `CPU` 不受支持时——显示错误信息，并在 `Windows` 上打开修复用的 `URL`
