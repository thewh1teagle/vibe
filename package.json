{
	"private": true,
	"packageManager": "pnpm@10.4.1",
	"scripts": {
		"dev": "cd desktop && pnpm tauri dev",
		"build": "cd desktop && pnpm tauri build",
		"format": "prettier --write .",
		"format:check": "prettier --check .",
		"check-types": "tsc --noEmit -p desktop/tsconfig.json"
	},
	"devDependencies": {
		"prettier": "^3.5.3",
		"typescript": "^5.9.3"
	}
}
